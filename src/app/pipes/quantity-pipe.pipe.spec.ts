import { QuantityPipePipe } from './quantity-pipe.pipe';

describe('QuantityPipePipe', () => {
  it('create an instance', () => {
    const pipe = new QuantityPipePipe();
    expect(pipe).toBeTruthy();
  });
});
